global:
  scrape_interval: 5s

scrape_configs:
  - job_name: 'fastapi_engine_remote'
    metrics_path: '/metrics'
    static_configs:
      # We are now scraping the remote Tencent Cloud (Data Plane) from the AWS Sydney Cloud (Control Plane)
      - targets: ['100.88.60.48:8001']
