version: '3.8'

services:
  db:
    image: ankane/pgvector:latest
    container_name: enterprise-rag-db
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: postgres
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
      - ./db/init.sql:/docker-entrypoint-initdb.d/init.sql:ro
    restart: always

  engine:
    build: ./engine
    container_name: enterprise-rag-engine
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY}
      - DATABASE_URL=postgresql://postgres:postgres@db:5432/postgres
      - HTTP_PROXY=socks5://host.docker.internal:1080
      - HTTPS_PROXY=socks5://host.docker.internal:1080
      - ALL_PROXY=socks5://host.docker.internal:1080
    ports:
      - "8001:8001"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    depends_on:
      - db
    restart: always

  gateway:
    build: ./gateway
    container_name: enterprise-rag-gateway
    environment:
      - ENGINE_URL=http://engine:8001
      - PORT=8000
    ports:
      - "80:8000"
    depends_on:
      - engine
    restart: always

volumes:
  pgdata:
